const wordsArray = ["monkey",
                    "lion",
                    "bear",
                    "hippopotamus",
                    "tiger",
                    "sloth"]

const secretWord = wordsArray[Math.floor(Math.random()*wordsArray.length)];
export default secretWord
